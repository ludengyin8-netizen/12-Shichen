Phase 9 continued: implement SwiftAA-backed lunar altitude and event refinement under #if canImport(SwiftAA). The implementation uses a coarse sampling followed by bisection refinement to locate rise/set times and approximates transit as the maximum altitude within the sample window. If SwiftAA is unavailable at build, the engine returns UNAVAILABLE (nil) values to avoid fabricating data.
